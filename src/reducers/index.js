import { combineReducers } from "redux";

export default combineReducers({
  // Below, just a dummy func to return something, so Redux doesn't give errors during intial stages of building the app.
  replaceMe: () => "Dummy text",
});
